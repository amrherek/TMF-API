server:
    port: 8082
management:
    context-path: /manage

spring:
    application:
        name: obw-product-catalog
    cloud:
        config:
            enabled: false
            discovery:  
                enabled: false
eureka:
  client:
    enabled: false

bscs:
    provider: file